import React, { useState, useEffect, useCallback, useMemo, useContext } from 'react';
import Styled from 'styled-components';
import { Store } from '../App';

const Database = () => {
  const store = useContext(useContext);

  return (
    <></>
  )

}

export default Database;
